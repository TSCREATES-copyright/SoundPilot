/**
 * Deprecated in Step 2.
 * Storage has moved to Firebase Auth + Firestore.
 * Keep this module only as a compatibility stub.
 */

export function getUsers(): never[] {
  return []
}

export function saveUsers(): void {
  return
}

export function getSession(): null {
  return null
}

export function saveSession(): void {
  return
}

export function clearSession(): void {
  return
}
